export interface Repository {
    id: number
    name: string
    html_url: string
    created_at: string
    pushed_at: string
    language: string
    description: string
    fork: boolean
    stargazers_count: number
    archived: boolean
}
